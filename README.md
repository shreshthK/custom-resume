# Custom Resume

Monorepo for AI resume tailoring:
- `apps/web`: React + Vite frontend
- `apps/api`: NestJS backend
- `packages/types`: shared TypeScript types

## Quick start

```bash
pnpm install
pnpm dev:web
pnpm dev:api
```
